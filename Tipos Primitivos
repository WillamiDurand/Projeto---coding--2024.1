## Tipos Primitivos

## Strings com Template Literals - Representa textos, como palavras ou frases. Sempre delimitada por aspas simples ('), duplas (") ou crases (`) para template literals.
Permite interpolar variáveis em uma string de forma mais legível.

const nome = "Willami"; // String
const cidade = "Recife"; // String
console.log(`Meu nome é ${nome} e sou de ${cidade}`);

## Number
Usados para representar valores numéricos, incluindo inteiros e decimais.
const numero1 = 30; // Number: Um valor numérico.
const numero2 = 20; // Number: Outro valor numérico.

## Realizando operações básicas:
const soma = numero1 + numero2; // Soma
console.log(`O resultado da soma é: ${soma}`);

const sub = numero1 - numero2; // Subtração
console.log(`O resultado da subtração é: ${sub}`);

const mult = numero1 * numero2; // Multiplicação
console.log(`O resultado da multiplicação é: ${mult}`);

```const div = numero1 / numero2; // Divisão
console.log(`O resultado da divisão é: ${div}`);


## Média: Cálculo de médias usando números:

```const nota1 = 9; // Primeira nota (Number)
const nota2 = 7; // Segunda nota (Number)
const nota3 = 5; // Terceira nota (Number)
const media = (nota1 + nota2 + nota3) / 3; // Média das notas
console.log(`O resultado da média é ${media}`);
```
## Cálculos Financeiros

```Usando percentuais para desconto:
const valororiginal = 100; // Valor original (Number)
const percentualdesconto = 20; // Percentual de desconto (Number)
const desconto = (valororiginal * percentualdesconto) / 100; // Valor do desconto
const preco = valororiginal - desconto; // Valor final com desconto
console.log(`O valor do produto com desconto é: ${preco}`);
```
##Imposto de renda:

```const salario = 3000; // Salário bruto (Number)
const aliquota = 0.15; // Alíquota do imposto (Number)
const imposto = salario * aliquota; // Cálculo do imposto
console.log(`O imposto a ser pago é de R$ ${imposto}`);
```
## Conversores
De moedas (reais para dólares):
const real = 100; // Quantidade em reais (Number)
const dolar = 5.44; // Cotação do dólar (Number)
const conv = real * dolar; // Conversão
console.log(`Na cotação atual, seus 100 reais valem ${conv} dólares americanos`);
```
## De Celsius para Fahrenheit:

```const cel = 28; // Temperatura em Celsius (Number)
const far = (cel * 9 / 5) + 32; // Fórmula de conversão
console.log(`28° Celsius são ${far} Fahrenheit`);
```
## IMC (Índice de Massa Corporal): Calcula o IMC com base no peso e altura:

```const peso = 80; // Peso em kg (Number)
const altura = 1.80; // Altura em metros (Number)
const imc = peso / (altura * altura); // Fórmula do IMC
console.log(`O IMC é ${imc.toFixed(2)}. Seu peso está normal`);
```








